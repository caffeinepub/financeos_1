# Growfinfire Global

## Current State
Portfolio page has Overview + 8 asset module tabs with table/card toggle (table default).
Goals page has Track Goals with table/card toggle. Card view exists but layout differs from target.

## Requested Changes (Diff)

### Add
- Nothing

### Modify
- Portfolio: Default view = card. Card layout: Name+badge left, metrics row, right=Allocation%+bar+Edit/Delete.
- Goals Track Goals: Default view = card. Card layout per reference image with circular ring, metrics, tags, off-white right panel.

### Remove
- Nothing

## Implementation Plan
1. Update Portfolio card default and rebuild card row per reference image
2. Update Goals card default and rebuild goal card per reference image
3. Preserve all toggles, CRUD, charts, summary cards, 20-year forecast tables
